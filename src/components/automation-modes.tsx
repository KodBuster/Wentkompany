/**
 * Что делает щит управления и диспетчеризации.
 *
 * Три сценария взяты из описания изделия у производства и переписаны
 * инженерным языком: исходная формулировка «защита от дурака» на сайт
 * не годится — это читают шеф-повар и проектировщик.
 *
 * Каждый сценарий помечен пунктом СП, который он закрывает: щит — не
 * дополнительная опция, а то, чем требование п. 5.30 закрывается физически.
 */

const MODES = [
  {
    n: '01',
    clause: 'п. 5.30',
    title: 'Падение давления воды',
    text:
      'Сигнализатор следит за давлением в линии, питающей гидроконтур. Если давление упало ниже ' +
      'рабочего или вода пропала совсем, включается световой и звуковой сигнал: работу аппарата ' +
      'нужно прекратить до устранения неисправности.',
  },
  {
    n: '02',
    clause: 'п. 5.30',
    title: 'Дублирование по температуре',
    text:
      'Если контроль давления не сработал, остаётся второй независимый контур: датчики температуры ' +
      'на входе в фильтр. Без воды температура продуктов горения растёт — датчик фиксирует рост ' +
      'и поднимает тот же сигнал.',
  },
  {
    n: '03',
    clause: 'п. 5.30',
    title: 'Блокировка повторного пуска',
    text:
      'После любого из срабатываний повторный запуск невозможен, пока неисправность не устранена. ' +
      'Смена не может «просто перезапустить» систему и работать дальше с неработающей фильтрацией.',
  },
];

export function AutomationModes() {
  return (
    <div className="tiles" style={{ gridTemplateColumns: 'repeat(auto-fit,minmax(280px,1fr))' }}>
      {MODES.map((m) => (
        <article key={m.n} className="tile">
          <div className="flex items-baseline gap-3">
            <span className="num text-2xl" style={{ color: 'var(--color-steel-500)' }}>{m.n}</span>
            <span className="lbl" style={{ color: 'var(--color-extract)' }}>{m.clause}</span>
          </div>
          <h3 style={{ textTransform: 'none', fontFamily: 'var(--font-body)', fontWeight: 600, fontSize: '1.05rem' }}>
            {m.title}
          </h3>
          <p className="muted text-sm">{m.text}</p>
        </article>
      ))}
    </div>
  );
}
